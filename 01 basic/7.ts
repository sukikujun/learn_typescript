{
    let ys = 'yurushi';
    ys = 'nibiiro yurushi';
    let sy = 100;
    // sy = true;

    let flag:boolean = true;
    flag = 1 > 2;

    function sum(a:number, b:number): string {
        return 'result:' + a  + b;
    }

    console.log(sum(3, 7));
}